sudo cp flake.nix /etc/nixos/

sudo cp configuration.nix /etc/nixos/
sudo cp home.nix /etc/nixos/

sudo cp configuration.desktop.nix /etc/nixos/
sudo cp configuration.wsl.nix /etc/nixos/
sudo cp configuration.shared.nix /etc/nixos/
sudo cp module-template.nix /etc/nixos/

sudo cp hardware-configuration.nix /etc/nixos/
sudo cp nvf-configuration.nix /etc/nixos/
